机房机子不够用，这是我在家做的。

在W3Schools和谷歌的帮助下，完成了ASP部分的编写，包括连接数据库，建立重复区域（其实是做了个记录集的属性遍历），以及从数据库中提取用户名(SQL的SELECT FROM)，密码的匹配，最后通过Response.Redirect对页面进行重定向实现跳转。

网页部分，用了Bootstrap，这个很简单。同时用了jQuery来实现最开始的密码为空的检验。

在这过程中还尝试用ASP和jQuery实现AJAX，然而以我的水平来说还是太复杂了点，能把ASP和SQL写好不出BUG就很不容易了XD